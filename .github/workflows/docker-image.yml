# git-ignore/docker-compose.yml
version: '3.8'

services:
  web:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: lanny-werberth-site
    ports:
      - "8080:80"
    restart: unless-stopped
